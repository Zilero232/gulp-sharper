export interface GulpRefilenameOptions {
  dirname?: string;
  basename?: string;
  prefix?: string;
  suffix?: string;
  extname?: string;
  multiExt?: boolean; // Для поддержки файлов с множественными расширениями
}
